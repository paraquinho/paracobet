import { Shell } from "@/components/shell";
import { getMarkets } from "@/lib/api";

export default async function MarketsPage() { const markets = await getMarkets(); return <Shell><p className="eyebrow">Immutable price snapshots</p><h1 className="mt-2 text-3xl font-semibold text-white">Mercados</h1><p className="mt-2 text-sm text-slate-400">Cada cambio de cuota crea un snapshot; los históricos no se sobrescriben.</p><div className="panel mt-6 overflow-hidden"><table className="w-full text-left text-sm"><thead className="border-b border-slate-800 text-xs uppercase tracking-wider text-slate-500"><tr><th className="px-5 py-3">Mercado</th><th className="px-5 py-3">Selección</th><th className="px-5 py-3">Línea</th><th className="px-5 py-3">Cuota</th><th className="px-5 py-3">Fuente</th></tr></thead><tbody>{markets.map(item => <tr key={item.id} className="border-b border-slate-800 last:border-0"><td className="px-5 py-4">{item.market}</td><td className="px-5 py-4 text-slate-300">{item.selection}</td><td className="px-5 py-4 text-slate-400">{item.line ?? "—"}</td><td className="px-5 py-4 font-semibold text-cyan-300">{item.odds.toFixed(2)}</td><td className="px-5 py-4 text-amber-300">{item.source}</td></tr>)}</tbody></table></div></Shell>; }
