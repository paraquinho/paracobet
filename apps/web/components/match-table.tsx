import Link from "next/link";
import type { Match } from "@/lib/types";

export function MatchTable({ matches }: { matches: Match[] }) {
  return <div className="panel overflow-hidden"><div className="flex items-center justify-between border-b border-slate-800/80 px-5 py-4"><div><p className="eyebrow">Feed de eventos</p><p className="mt-1 text-sm text-slate-400">Partidos disponibles para análisis</p></div><span className="rounded-full border border-slate-700 bg-slate-950/40 px-2.5 py-1 text-[11px] text-slate-400">{matches.length} eventos</span></div><div className="overflow-x-auto"><table className="w-full min-w-[640px] text-left text-sm"><thead className="border-b border-slate-800 bg-slate-950/50 text-xs uppercase tracking-wider text-slate-500"><tr><th className="px-5 py-3">Inicio</th><th className="px-5 py-3">Competición</th><th className="px-5 py-3">Partido</th><th className="px-5 py-3">Estado</th><th className="px-5 py-3">Fuente</th></tr></thead><tbody>{matches.map(match => <tr key={match.id} className="border-b border-slate-800/70 last:border-0 hover:bg-cyan-400/[0.03]"><td className="px-5 py-4 text-slate-400">{new Intl.DateTimeFormat("es", { dateStyle: "short", timeStyle: "short" }).format(new Date(match.starts_at))}</td><td className="px-5 py-4 text-slate-400">{match.competition}</td><td className="px-5 py-4 font-medium text-slate-100"><Link className="hover:text-cyan-300" href={`/matches/${match.id}`}>{match.home_team} <span className="text-slate-500">vs</span> {match.away_team}</Link></td><td className="px-5 py-4"><span className="rounded-full border border-cyan-500/20 bg-cyan-500/10 px-2 py-1 text-xs text-cyan-300">{match.score ?? match.status}</span></td><td className="px-5 py-4 text-xs text-amber-300">{match.source}</td></tr>)}</tbody></table></div></div>;
}
